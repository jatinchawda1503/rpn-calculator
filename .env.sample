# Database configuration
POSTGRES_USER=username
POSTGRES_PASSWORD=password
POSTGRES_DB=rpn_calculator

# Backend API configuration
API_PORT=8000
API_HOST=0.0.0.0
DATABASE_URL=postgresql://username:password@db:5432/rpn_calculator
FRONTEND_URL=http://localhost:3000
LOG_LEVEL=INFO

# Frontend configuration 
NEXT_PUBLIC_API_URL=http://localhost:8000

# Test database configuration
TEST_DATABASE_URL=postgresql://username:password@db:5432/rpn_calculator_test

# This is a sample environment file for Docker Compose configuration.
# Copy this to .env and replace with actual values for your environment.
# Note: Individual components (backend/frontend) may also have their own .env files
# with more specific configurations.
