# Database configuration for the core module
DATABASE_URL=postgresql://username:password@hostname:port/database_name

# This is a sample environment file for the core module.
# Copy this to .env and replace with actual values for your environment.
# The DATABASE_URL is used for connecting to PostgreSQL database. 